[project]
name = "lookahead-examples"
version = "0.1.0"
description = ""
authors = [
    {name = "TensorRT LLM [svc_tensorrt_llm@nvidia.com]"}
]
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    "datasets (==3.1.0)",
    "rouge-score (>=0.1.2,<0.2.0)",
    "sentencepiece (>=0.1.99)",
    "evaluate (>=0.4.5,<0.5.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
