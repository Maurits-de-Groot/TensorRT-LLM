[project]
name = "triton-backend"
version = "0.1.0"
description = ""
authors = [
    {name = "TensorRT LLM [svc_tensorrt_llm@nvidia.com]"}
]
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    "regex (>=2025.7.34,<2026.0.0)",
    "fire (>=0.7.1,<0.8.0)",
    "tritonclient[all] (>=2.60.0,<3.0.0)",
    "transformers (==4.51.0)",
    "pandas (>=2.3.2,<3.0.0)",
    "tabulate (>=0.9.0,<0.10.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
